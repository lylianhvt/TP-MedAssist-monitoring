{
  "title": "HAProxy & API High Availability",
  "panels": [
    {
      "type": "stat",
      "title": "Instances actives (sur 3)",
      "targets": [
        {
          "expr": "sum(haproxy_server_up{backend=\"apis\"})",
          "legendFormat": "actives"
        }
      ],
      "gridPos": {"h": 4, "w": 8, "x": 0, "y": 0}
    },
    {
      "type": "graph",
      "title": "Sante individuelle des backends",
      "targets": [
        {
          "expr": "haproxy_server_up{backend=\"apis\"}",
          "legendFormat": "{{server}}"
        }
      ],
      "gridPos": {"h": 8, "w": 12, "x": 8, "y": 0}
    },
    {
      "type": "graph",
      "title": "Basculements (echecs de checks sur 1h)",
      "targets": [
        {
          "expr": "increase(haproxy_server_check_failures_total{backend=\"apis\"}[1h])",
          "legendFormat": "{{server}}"
        }
      ],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 4}
    },
    {
      "type": "graph",
      "title": "Echecs cumules des health checks",
      "targets": [
        {
          "expr": "haproxy_server_check_failures_total{backend=\"apis\"}",
          "legendFormat": "{{server}}"
        }
      ],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 8}
    }
  ],
  "schemaVersion": 27,
  "version": 2,
  "refresh": "10s",
  "datasource": null
}